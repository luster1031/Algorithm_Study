#include <iostream>
#include <vector>
#include<math.h>

using namespace std;

int main() {
	int input;
	cin >> input;
	
	for (int i = 2; i <= input;) {
		if (input % i == 0) {
			cout << i << '\n';
			input /= i;
		}
		else {
			i++;
		}
	}
	
}
