#include<iostream>
#include<vector>
#include <algorithm>

using namespace std;
int main() {
	vector<int> input;
	int testcase,num;

	cin >> testcase;
	for(int i  = 0; i<testcase; i++){
		cin >> num;
		input.push_back(num);
	}
	sort(input.begin(), input.end());
	for (int i = 0; i < testcase; i++) {
		cout << input[i] << '\n';
	}
	return 0;
}
