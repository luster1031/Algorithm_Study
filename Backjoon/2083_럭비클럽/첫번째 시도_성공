#include<stdio.h>

int main() {
	char name[11];
	int age, wight;
	scanf("%s %d %d", name, &age, &wight);
	while (age != 0) {
		if (age > 17 || wight >= 80) {
			printf("%s Senior\n", name);
		}
		else {
			printf("%s Junior\n", name);
		}
		scanf("%s %d %d", name, &age, &wight);
	}

	return 0;
}
