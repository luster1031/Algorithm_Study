//행안에서 숫자 찾는거 중에서 최소값의 큰 값을 구하고
//큰 값에서 정렬


    //회원 수 받기 <= 50
		//-1이 나올 때 까지 입력받기
      //input[one][카운트 배열]
			//input[two][카운트 배열]
			//카운터배열[one]++;
			//카운터배열[two]++;

	//회원 수 만큼
		//for(1 ~ 회원 수 까지)
			//row_max = 0;
			//row_col_count = 0 행에서 각 숫자에 해당하는 변수 카운터

			//for(1 ~ 회원 수 ) -> 숫자
				//행()
				//최대값 구하기, 

			//정렬하면서 넣기

}

//함수(행, 값)
	//각 행에 원하는 숫자가 있으면
		//count++
		//return 

	//각 행에 원하는 숫자가 없으면 
		//for(행에 들어있는 데이터 개수 만큼)
			//함수(행, 값)
